type User {
  id: ID! @id,
  name: String,
  email: String! @unique,
  password: String!,
  createdAt: DateTime! @createdAt,
  updatedAt: DateTime! @updatedAt,
}

type Randoms {
  id: ID! @id,
  userId: ID!,
  valid: Boolean,
  createdAt: DateTime! @createdAt,
}